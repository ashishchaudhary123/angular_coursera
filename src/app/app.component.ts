import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})

  // so that this component can be imported !!
export class AppComponent {
  title = 'conFusion';
}
